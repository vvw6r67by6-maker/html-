简体中文：
确保你已安装依赖 (pywebview, pyinstaller)，命令：pip install pywebview pyinstaller，不会用命令问deepseek
将文件解压到桌面，右键，点在终端打开，运行命令html打包器.exe 你的网页.html 生成的可执行文件名.exe

English：
Make sure you have installed the dependencies (pywebview, pyinstaller). Command: pip install pywebview pyinstaller. If you don't know the command, ask DeepSeek.
Extract the file to your desktop, right‑click, select "Open in terminal", and run the command:
html打包器.exe your_webpage.html output_executable_name.exe
繁體中文：
請確認您已安裝依賴套件 (pywebview, pyinstaller)，指令：pip install pywebview pyinstaller
若不知道指令，請詢問 DeepSeek。
將檔案解壓縮到桌面，按滑鼠右鍵，選擇「在終端機中開啟」，然後執行指令：
html打包器.exe 你的網頁.html 生成的可執行檔名.exe
